version: '3'

services:

  db:
    image: mysql:5.7
    ports:
      - 3306:3306
    environment:
      - MYSQL_DATABASE=app
      - MYSQL_USER=letsfind
      - MYSQL_PASSWORD=letsfind
      - MYSQL_ROOT_PASSWORD=letsfind
      - restart= always
