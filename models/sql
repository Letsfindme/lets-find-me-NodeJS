CREATE TRIGGER update_post_rate AFTER 
UPDATE
   ON postrates FOR EACH ROW 
      UPDATE
         posts 
      SET
         starCount = 
            (SELECT
               AVG(rate) 
            from
               postrates 
            where
               postrates.postId = postrates.postId)
	  where
		   posts.id = NEW.postId
;